🎮 Number Guessing Game in C
📖 Overview
This project is a simple number guessing game written in C.
The program generates a random number between 1 and 100, and the player must guess it.
It provides hints ("larger" or "smaller") until the correct number is guessed, and displays the total number of attempts.
✨ Features
- Random number generation using rand() seeded with time(NULL)
- User input for guesses between 1–100
- Feedback:
- "Guess larger number" if the guess is too low
- "Guess smaller number" if the guess is too high
- Tracks number of attempts until success
- Friendly exit message and developer credit

🛠️ Technologies Used
- Language: C
- Compiler: GCC / MinGW / Turbo C (any standard C compiler)
